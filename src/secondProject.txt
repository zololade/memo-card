========================================
FILE: ./App.css
========================================
.gameBoard {
  /* 1. Rigid Grid Layout: Keeps your board structure immaculate */
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1.5rem;
  max-width: 1200px;
  margin: 2rem auto;
  padding: 0 1rem;

  .cardPiece {
    /* 2. Glassmorphism Card Feel */
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(8px);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 16px;
    padding: 12px;
    box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.2);

    /* Layout inside the card */
    display: flex;
    flex-direction: column;
    align-items: center;
    cursor: pointer;

    /* 3. Smooth Hover Mechanics */
    transition:
      transform 0.25s cubic-bezier(0.4, 0, 0.2, 1),
      box-shadow 0.25s ease,
      border-color 0.25s ease;

    &:hover {
      transform: translateY(-8px) scale(1.03);
      border-color: rgba(255, 215, 0, 0.6); /* Cyber yellow glow */
      box-shadow: 0 12px 40px 0 rgba(255, 215, gold, 0.25);
    }

    &:active {
      transform: translateY(-2px) scale(0.98);
    }

    /* 4. Aspect-Ratio Locked Image Box */
    .image-container {
      position: relative; /* Keeps the absolute skeleton locked inside */
      width: 100%;
      aspect-ratio: 1 / 1;
      background: var(--border); /* Falls back to your variable base color */
      border-radius: 12px;
      overflow: hidden;

      img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition:
          opacity 0.3s ease,
          transform 0.3s ease; /* Fades image in smoothly */
      }
    }

    /* 1. Shimmering Skeleton Box */
    .skeleton-box {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, var(--code-bg) 25%, var(--border) 50%, var(--code-bg) 75%);
      background-size: 200% 100%;
      animation: shimmer 1.5s infinite linear;
    }

    .skeleton-text {
      width: 70%;
      height: 1rem;
      margin-top: 0.75rem;
      border-radius: 4px;
      background: linear-gradient(90deg, var(--code-bg) 25%, var(--border) 50%, var(--code-bg) 75%);
      background-size: 200% 100%;
      animation: shimmer 1.5s infinite linear;
    }

    /* Optional text wrapper if your cards have labels/names underneath */
    .card-title {
      margin-top: 0.75rem;
      font-size: 1rem;
      font-weight: 600;
      color: #ffffff;
      text-transform: capitalize;
      letter-spacing: 0.5px;
    }
  }
}

@keyframes shimmer {
  0% {
    background-position: 200% 0;
  }
  100% {
    background-position: -200% 0;
  }
}


========================================
FILE: ./App.tsx
========================================
import { useState } from "react";

import "./App.css";
import { GameBoard } from "./components/GameBoard";
import { GameStats } from "./components/GameStats";

const initialBoardState = {
  currentScore: 0,
  highestScore: 0,
  resetGameId: crypto.randomUUID(),
};

function App() {
  const [boardState, setBoardState] = useState(initialBoardState);

  function currentScoreHnd() {
    setBoardState((prev) => {
      return { ...prev, currentScore: prev.currentScore + 1 };
    });
  }

  function resetGameHnd() {
    setBoardState((prev) => {
      return {
        ...prev,
        currentScore: 0,
        highestScore:
          boardState.currentScore > boardState.highestScore ? prev.currentScore : prev.highestScore,
        resetGameId: crypto.randomUUID(),
      };
    });
  }

  return (
    <>
      <GameStats boardState={boardState} />
      <GameBoard
        updateCurrScore={currentScoreHnd}
        resetGame={resetGameHnd}
        key={boardState.resetGameId}
      />
    </>
  );
}

export default App;


========================================
FILE: ./components/CardPiece.tsx
========================================
import { type GifResult } from "@giphy/js-fetch-api";
import { useState, type MouseEvent } from "react";

import type { GameBoardProp } from "./GameBoard";

function CardPiece({
  updateCurrScore,
  resetGame,
  val,
  shuffle,
}: GameBoardProp & { val: GifResult["data"]; shuffle: () => void }) {
  const [clickedState, setClickedState] = useState(false);
  const [isImageLoaded, setIsImageLoaded] = useState(false);

  function handleClick(_e: MouseEvent) {
    if (clickedState === false) {
      setClickedState(true);
      updateCurrScore();
      shuffle();
      return;
    }

    resetGame();
  }

  return (
    <div className="cardPiece" onClick={handleClick}>
      <div className="image-container">
        {!isImageLoaded && <div className="skeleton-box" />}

        <img
          src={val.images.fixed_height_still.url}
          alt={val.title || "Game card"}
          onLoad={() => setIsImageLoaded(true)}
          style={{ opacity: isImageLoaded ? 1 : 0 }}
          loading="lazy"
        />
      </div>

      {!isImageLoaded ? (
        <div className="skeleton-text" />
      ) : (
        <span className="card-title">{val.title || "Call of Duty Asset"}</span>
      )}
    </div>
  );
}
export { CardPiece };


========================================
FILE: ./components/GameBoard.tsx
========================================
import { type GifResult, GiphyFetch } from "@giphy/js-fetch-api";
import { useEffect, useState } from "react";

import { CardPiece } from "./CardPiece";

interface GameBoardProp {
  updateCurrScore: () => void;
  resetGame: () => void;
}

const gf = new GiphyFetch(import.meta.env.VITE_GIPHY_KEY);
const searchTerm = "call of duty";

function GameBoard(props: GameBoardProp) {
  const [gifs, setGifs] = useState<GifResult["data"][]>([]);

  function handleShuffle() {
    setGifs((prev) => [...shuffleArray(prev)]);
  }

  useEffect(() => {
    let isMounted = true;

    const fetchData = async () => {
      try {
        const randomOffset = Math.floor(Math.random() * 150);
        const { data: gifs } = await gf.search(searchTerm, { limit: 10, offset: randomOffset });
        if (isMounted) {
          setGifs(gifs);
        }
      } catch (err) {
        console.error(err);
      }
    };

    fetchData();

    return () => {
      isMounted = false;
    };
  }, []);

  return (
    <section className="gameBoard">
      {gifs.map((val) => (
        <CardPiece {...props} key={val.id} val={val} shuffle={handleShuffle} />
      ))}
    </section>
  );
}

//helper
function shuffleArray<T>(array: T[]) {
  for (let i = array.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [array[i], array[j]] = [array[j], array[i]];
  }
  return array;
}

export { GameBoard, type GameBoardProp };


========================================
FILE: ./components/GameStats.tsx
========================================
import type { BoardState } from "../type";

interface Prop {
  boardState: BoardState;
}

function GameStats({ boardState }: Prop) {
  return (
    <>
      <p>{boardState.currentScore}</p>
      <p>{boardState.highestScore}</p>
    </>
  );
}

export { GameStats };


========================================
FILE: ./index.css
========================================
/* ==========================================================================
   1. Reset & Global Defaults
   ========================================================================== */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  overflow-wrap: anywhere; /* Replaces legacy word-wrap & handles long URLs better */

  /* Modern Nesting syntax without ampersand duplication */
  &:not(textarea) {
    scrollbar-gutter: stable both-edges;
  }
}

html {
  /* Dynamic page scrolling adjustments for general modern feel */
  scroll-behavior: smooth;
  scroll-padding-top: 2rem;
}

body {
  min-height: 100vh; /* Ensures full height layout containers work */
  text-rendering: optimizeSpeed; /* Better rendering performance than optimizeLegibility */
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

fieldset {
  border: none;
  margin: 0;
  padding: 0;
}

/* ==========================================================================
   2. Design Tokens (CSS Variables)
   ========================================================================== */
:root {
  /* Light Theme Archetype */
  --text: #524a59; /* Slightly darker for enhanced readability contrast */
  --text-h: #0b0813;
  --bg: #ffffff;
  --border: #e2e0e5;
  --code-bg: #f5f4ef;
  --accent: #9313ff; /* Shifted hue slightly for pure AA accessibility contrast */
  --accent-bg: rgba(147, 19, 255, 0.08);
  --accent-border: rgba(147, 19, 255, 0.4);
  --social-bg: rgba(245, 244, 239, 0.5);
  --shadow: 0 10px 15px -3px rgba(11, 8, 19, 0.08), 0 4px 6px -2px rgba(11, 8, 19, 0.04);

  /* Typography Stack Configuration */
  --sans:
    system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
  --heading: var(--sans);
  --mono: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;

  /* Global baseline options */
  font-family: var(--sans);
  font-size: 18px;
  line-height: 1.5; /* Pure unitless line height translates cleaner to nested children */
  letter-spacing: 0.01em; /* Percentage or em tracking scales better than fixed pixels */
  color-scheme: light dark;
  color: var(--text);
  background-color: var(--bg);
  font-synthesis: none;

  /* Fluid Typography for screens under 1024px width */
  @media (max-width: 1024px) {
    font-size: 16px;
  }
}

/* ==========================================================================
   3. Dark Mode Theme Context
   ========================================================================== */
@media (prefers-color-scheme: dark) {
  :root {
    --text: #9fa6b2;
    --text-h: #f9fafb;
    --bg: #0f1015; /* Richer, deeper midnight base for better OLED display blending */
    --border: #23252e;
    --code-bg: #191a21;
    --accent: #c084fc;
    --accent-bg: rgba(192, 132, 252, 0.12);
    --accent-border: rgba(192, 132, 252, 0.4);
    --social-bg: rgba(35, 37, 46, 0.5);
    --shadow: 0 10px 20px -3px rgba(0, 0, 0, 0.5), 0 4px 8px -2px rgba(0, 0, 0, 0.3);
  }
}

/* ==========================================================================
   4. Typography Semantics
   ========================================================================== */
h1,
h2,
h3,
h4,
h5,
h6 {
  font-family: var(--heading);
  font-weight: 600; /* Bumped to 600 for punchier typography hierarchy */
  color: var(--text-h);
  line-height: 1.2;
}

h1 {
  font-size: clamp(2.25rem, 5vw, 3.5rem); /* Automated size scaling between 36px and 56px */
  letter-spacing: -0.03em;
  margin-block: 1.5rem; /* Uses logical properties for cleaner top/bottom margin rendering */
}

h2 {
  font-size: clamp(1.25rem, 3vw, 1.5rem); /* Automated size scaling between 20px and 24px */
  letter-spacing: -0.015em;
  margin-block-end: 0.5rem;
}

p {
  margin-block-end: 1rem; /* Standard paragraph separation spacing to avoid clustered runs */
}


========================================
FILE: ./main.tsx
========================================
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";

import "./index.css";
import App from "./App.tsx";

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);


========================================
FILE: ./secondProject.txt
========================================


========================================
FILE: ./type.ts
========================================
interface BoardState {
  currentScore: number;
  highestScore: number;
  resetGameId: string;
}

export type { BoardState };


